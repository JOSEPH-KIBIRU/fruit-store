export const initiateSTKPush = async (phoneNumber, amount, orderId) => {
  // This would integrate with MPESA API
  console.log(`Initiating MPESA payment of KSh ${amount} to ${phoneNumber} for order ${orderId}`);
  // Actual implementation would use Daraja API
};